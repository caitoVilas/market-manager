server:
  port: 30000
  servlet:
    context-path: /mm/api
spring:
  application:
    name: market-manager
## datasource:
  datasource:
    url: jdbc:mysql://localhost:3306/mm
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true